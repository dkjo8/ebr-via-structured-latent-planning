# Optional uv project metadata (tags, default log directory). Not required to run Julia experiments.
[uv]
project = "ebrm"
description = "Energy-Based Reasoning via Structured Latent Planning"
backend = "file"
log_dir = "runs"

[uv.tags]
framework = "Flux.jl"
language = "Julia"
domain = "reasoning"
